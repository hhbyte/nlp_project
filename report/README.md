# Report

The final written report is in `final_report/`.

Important files:

- `final_report/fnlp_project_report.pdf` - submitted report PDF.
- `final_report/fnlp_project_report.tex` - LaTeX source.
- `final_report/utils/` - minimal TAU template files needed to compile the
  source.

The old template working directory is stored under `archive/report_template_working/`
for provenance.
