# Model Artifacts

Use this folder for local model artifacts, checkpoints, vectorizers, and other
generated files that should not live inside the source package.

Large generated artifacts should stay out of Git unless the team explicitly
decides otherwise.
